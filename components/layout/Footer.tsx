import type { Locale } from '@/lib/i18n'

/**
 * PLACEHOLDER — Task 4 (Header, Footer, chuyển ngôn ngữ, menu mobile) thay
 * bằng component thật. Xem ghi chú ở `Header.tsx` cùng thư mục.
 */
export function Footer(_props: { lang: Locale; navigation: unknown; settings: unknown }) {
  return null
}
